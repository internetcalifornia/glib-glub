/**
 * The product's display name, in exactly one place.
 *
 * No name has been chosen yet (PLATFORM.md, open questions). Everything that
 * shows a name — page titles, the tutor introducing itself, emails — reads it
 * from here, so choosing one later is a one-line change rather than a grep.
 */

export const PRODUCT_NAME = 'glib-glub';

/** How the tutor refers to itself in speech. Kept separate so a product rename
 *  and a tutor persona rename are independent decisions. */
export const TUTOR_NAME = 'your tutor';
